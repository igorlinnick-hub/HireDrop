# HANDOFF — ZipRecruiter / движок подачи

Обновлено: **2026-09-04** · ext **1.7.3** (репо и `~/Desktop/HireDrop-Ext` синхронны) ·
всё смержено в `main` (#106–#112 + #110)

> Igor: «не годится, что то работает, то не работает». Всё ниже — из живых прогонов
> (бэкенд-лог + дампы DOM). Ни одной строки по памяти: если написано «доказано» —
> есть улика, если «не доказано» — так и сказано.

---

## 1. Где мы

**ГЕЙТ ВЗЯТ 09-04.** Сквозная подача с записью доказана — то, чего лейн ждал три недели:

```
07:41:10  ✅ Applied: U.S. Social Media & Growth Specialist @ JustSpanish
07:43:32  ✅ Applied: Social Media and Marketing Manager @ Axios
07:47:28  ✅ Applied: Marketing Manager @ Dade Electric Service Inc
07:49:15  ✅ Applied: Social Media Marketing Specialist @ Best Practice Way LLC

campaign/status → today_applications = 4 · {'indeed': 4}
```

Обе улики сразу: строка в долговечном логе И записи в базе. **CWS-выкладка разблокирована.**

Тем же прогоном подтвердилось вживую, без отдельных проверок:
- **автопереключение платформ** (#104, висело непроверенным с 08-15):
  `ZipRecruiter exhausted (no more results) — switching to Indeed automatically`;
- **пустые страницы** больше не съедают минуты (две подряд закрывают ключевик);
- **Indeed на общем `phase3`** — формы на 2–3 шага, комбобоксы, радио, резюме: заполняются
  и уходят. Это был отдельный пункт плана, закрыт бесплатно;
- **инвариант держится**: где форма не приняла ответы — честный возврат, не тихая смерть.

Что осталось незакрытым по ZipRecruiter: **сам ZR в этом прогоне подач не дал** — его выдача
по «social media manager»/Miami кончилась на 5-й странице, и всё, что было, оказалось
external-apply. Механика ZR доказана раньше (2 заявки 08-15 в ZR → Applied), но подача
с записью ИМЕННО на ZR ещё не снята. Это НЕ блокер релиза: движок доказан на Indeed,
`phase3` общий.

## 2. Что было сломано (19 багов, все подтверждены живьём)

Три корня объясняют всё:

**(а) Движок жал не ту кнопку.** `findFormButton()` искал по всему документу, а строка
поиска ZipRecruiter — это `form button[type="submit"]`. Движок заполнял модалку и жал
**«Search»**, тот перезагружал выдачу поверх открытой формы. Снаружи — «форма открылась,
через 30 с уже следующая вакансия» (08-12, 08-14).

**(б) У ZipRecruiter НЕТ кнопки Submit** — последний шаг тоже называется «Continue».
Поэтому реальные подачи не записывались: ни счётчика, ни ключа дедупликации → движок
подал бы на ту же вакансию повторно.

**(в) Кампанию убивали снаружи посреди заполнения формы**, трижды по одной причине —
*сторож судил о жизни по сигналу, который жизнь не измеряет*:
- #98 — heartbeat мерил «расширение загружено», а не «кампания работает»;
- #107 — **второе, простаивающее расширение** пинговало «не работаю» под тем же аккаунтом
  и гасило чужую кампанию;
- #111 — TTL 150 с против собственного троттлинга MV3 (живая улика: **4 м 39 с** между
  двумя строками здорового обхода).

Остальные — в таблице:

| # | Баг | Фикс |
|---|---|---|
| 1 | Поиск кнопки по всему документу → «Search» в шапке ZR | Скоуп на модалку + deny-list `search/close/cancel/back/save & exit/skip` |
| 2 | У ZR несколько видимых `[role=dialog]`, форма — не первая | Перебираем все, сначала те, где есть поля |
| 3 | Кнопка шага «Continue Application» не матчилась | Матч по префиксу |
| 4 | Филлер заполнял только `required`, а у ZR его НЕТ ни на одном поле | Заполняем любое видимое пустое поле в модалке |
| 5 | Филлеры искали поля по всему документу | `formScope()` — модалка, если открыта |
| 6 | `waitForFormReady` видел страницу ЗА модалкой | Модаль-aware ожидание + ждём смену шага после клика |
| 7 | Последний «Continue» = submit, не распознавался | `detectSilentSubmission()` — по состоянию платформы |
| 8 | Уже поданные вакансии заново шли через fit-судью и письмо | Проверка «Applied» первой |
| 9 | Форма могла молча топтаться 20 кругов | Stall-guard: 2 круга без прогресса → `handBackJob` |
| 10 | Харвест ловил только «Quick apply», а ZR мешает его с **«1-click apply»** | Регексп принимает оба (≈половина выдачи была невидима) |
| 11 | Fit-судья и письмо звались ДО проверки нативной кнопки | Гейт нативной подачи первым (экономия денег) |
| 12 | Chrome при рестарте воскресил 9 старых окон автоматизации — работали разом | Автоматизирует только вкладка кампании; «нет данных» ≠ разрешение |
| 13 | Домашняя страница ZR распознавалась как форма | Не-джобовые пути не могут быть формой + сброс `currentJobInfo` на старте |
| 14–16 | Три heartbeat-бага (см. выше) | #98 / #107 / #111 |
| 17 | **Филлер падал молча** — исключение в `phase3` не ловил никто | try/catch + `💥 Form filler crashed: … @ файл:строка` |
| 18 | Филлер выпадал за модалку: на «однотаповой» вакансии 36 с крутил фильтры поиска позади формы и сам её закрывал | Все филлеры скоупятся, кастомные виджеты считаются полями |
| 19 | Возвраты не запоминались — тот же обход заново платил за fit+письмо (одни и те же 3 вакансии обработаны трижды за 20 минут) | Ключ «возвращено сегодня» (по дате) |

---

## 3. Главное техническое открытие: ZR = Downshift

Это то, обо что разбивались все попытки, и оно НЕ угадывается по коду — только живым дампом.

Вопросы «да/нет» у ZR и **обязательное** согласие в «однотаповой» форме — это
**Downshift**-комбобоксы:

```
DIV[role=combobox] name=['sms_opt_in'] vis=true exp=false txt="Open"
  → синтетический клик (pointerdown/mousedown/pointerup/mouseup/click): exp остаётся FALSE
  → ArrowDown:                                                          exp=true
  → меню = <ul> из aria-controls, у него offsetParent === null,
    внутри реальный <li role="option"> «I Agree»
  → без ответа шаг отказывает: «This field is required», aria-invalid=true
```

Отсюда два правила, которые легко потерять при рефакторинге:
1. **Открывать клавишей** (`ArrowDown`), клик — только как дополнение.
2. **Не требовать `offsetParent`** ни от меню, ни от опций: у ZR реальный, кликабельный
   список честно репортит «невидим».

Ещё два тайминга, добытых замером: модалка рендерится **~30 с** в фоновом окне (ждём 40 с,
пустую оболочку считаем external только через 12 с), а выдача кончается на 5-й странице —
две пустые подряд закрывают ключевик (раньше шли до 18-й по 25 с на страницу).

---

## 4. Что делать дальше — по шагам

**Шаг 1 — CWS-выкладка 1.7.3.** Гейт выполнен, держать её больше нечем. У пользователей
сейчас 1.4.4 — сломанная. Перед выкладкой: Игорь делает OFF/ON расширения (в репо и на
Рабочем столе уже 1.7.3, но в Chrome загружена 1.7.2 — версия меняется только тумблером).

**Шаг 2 — добить 20 заявок** для живых чисел (completion %, hand-back, receipts). Прогон
запускается за минуту, движок автономен: сам переключает платформы и сам возвращает
то, что не смог.

**Шаг 3 — снять подачу с записью на самом ZR.** Нужна выдача, где есть нативные вакансии:
пробовать другие ключевики/город, либо дождаться свежих постингов.

**Шаг 4 — адрес в профиле** (Settings → Mailing address). Миграция применена 08-21,
#110 смержен, поле ждёт значения. Без него ZR не пропускает контактный шаг у части вакансий.

## 5. Инструменты (tmp чистится — восстанавливать каждую сессию)

Мост в браузер: `osascript` + вкладка дашборда. Требует **хотя бы одно открытое окно
Chrome** — при нуле окон мост мёртв, и прогон обрывается (так умер прогон 08-21 22:39).

```bash
# 1. окно дашборда + его id
W=$(osascript -e 'tell application "Google Chrome"
set w to make new window
set URL of active tab of w to "https://hiredrop.io/dashboard"
return id of w
end tell')

# 2. токен Supabase лежит в COOKIES, не в localStorage:
#    sb-…-auth-token.0/.1, склеить, снять префикс base64-, atob → access_token
#    (вытащить через execute javascript в этой вкладке, положить в token.txt)

# 3. дальше всё через curl с этим токеном:
#    GET  /api/v1/activity?limit=200      — лог (единственный durable источник правды)
#    GET  /api/v1/campaign/status         — ЖИВОСТЬ кампании (running)
#    GET  /api/v1/extension/ping          — версия расширения + today_count
#    POST /api/v1/campaign/stop           — погасить
```

⚠️ **Живость кампании берётся из `/campaign/status`, а НЕ из `/extension/ping`.**
Пинг долгое время отдавал `campaign_running` из памяти воркера и врал, что живая кампания
остановлена — час погони за призраком. Починено (#107), но привычка важнее.

Старт кампании — только через мост `window.postMessage({type:'HIREDROP_START_CAMPAIGN', filters})`
из вкладки hiredrop.io: один `POST /campaign/start` расширение не запускает.

Активация правок: `background.js` → `HIREDROP_DEV_RELOAD` из вкладки дашборда; в этой цепочке
этого хватало и для `content.js` (окно автоматизации создаётся заново). Версию проверять
пингом, не «на глаз». После любой правки — `../scripts/sync-ext.sh`.

---

## 6. Ground truth

Правда о подачах — **только аккаунт платформы**, не наш дашборд:
- `ziprecruiter.com/candidate/my-jobs` → вкладка **Applied**
- `/candidate/my-jobs-in-progress` → **Incomplete** (следы брошенных попыток)
- `/jobseeker/applications` и `/jobseeker/my-jobs` редиректят на поиск — не путаться.

Свою вкладку ZR закрывать сразу: контент-скрипт инжектится и в неё.

---

## 7. Диагностика, которой раньше не было

Именно она превратила «месяцы вслепую» в «баг за 20 минут». Не удалять при чистках:

- `STEP N [платформа] filled=[…] btn="…" (dialog|page) → SUBMIT|continue dialogs=…` — на каждый шаг формы;
- `💥 Form filler crashed: <сообщение> @ <файл:строка>` — раньше исключение не оставляло следа;
- `Content script alive on … (ext 1.7.1)` — версия в каждой строке, устаревшая копия не спрячется;
- каждая остановка кампании называет себя (сервером / закрытой вкладкой / окном / вручную);
- `🔎 campaignRunning true → false (storage write)` — датчик на саму запись флага; именно он
  доказал, что флаг гасил ЧУЖОЙ экземпляр расширения;
- возврат человеку называет поля-блокеры (`unfilled: [...]`), и на платформах без `required`
  список строится из всех видимых пустых полей модалки.

**Правило лейна:** любая слепая зона движка закрывается durable-строкой ДО гипотез.
Решения, написанные только в попап, не существуют — попап умирает вместе с окном.
