# campaign-runtime

Обновлено: 2026-09-11 (поздний вечер) · ветка: main · ext в проде: **1.8.4**

## Состояние

Цикл: скрейп → фильтры → fit-гейт → walk по платформам → подача. Окно автоматизации **видимое**
(политика CWS), превью через CDP. Auto и Tap делят один движок; капы слоёные — серверная правда
`app/db/subscriptions.py`, клиентский ban-гейт с SAFE-дефолтом 20, на 429 стоп (#95).

Паузы-с-передачей-человеку, все по одной схеме (строка в окне + лента + `DETECTION_TRIPPED` +
авто-возврат + стоп через 2 ч): капча (#72), логин-стена, **стена согласия «Accept Terms»
(#182)**. Разница: капча в pool-режиме СКИПАЕТ пик (вакансие-специфична), стена согласия
ПАУЗИТ и там (аккаунт-широкая); Accept не жмём никогда — юр-действие владельца.
Hand-off несёт `kind`/`action` — дашборд (website #146) и попап различают копию.

## Последний заход (09-11, сессия A)

- **#182 (1.8.2)**: `detectConsentGate()` — видимый диалог, называющий terms И несущий кнопку
  accept. Ложняки отсечены: диалог с вводом = форма (чекбокс «I agree» в Lever/GH не стопит),
  `"privacy policy"` не триггер (куки-баннеры). 10 фикстур: `tests/consent-gate.test.js`.
- **#183 (1.8.3)**: DOM-канал `isDetected()` гейтился на `offsetParent !== null` — null у
  любого `position:fixed`, т.е. у модалок, включая собственные селекторы `captcha-modal`.
  Подтверждено симуляцией offsetParent по спеке. Заменено на `isVisibleBox` (строже: ловит
  и `visibility:hidden`); `querySelector` → `querySelectorAll` (скрытый шаблон заслонял
  настоящую). 8 фикстур: `tests/detection-visibility.test.js`.
- **Перепроверки**: tapMode fail-closed в проде с #171 (`background.js:1322`; флаг старта
  пишется в одном месте, ниже гейта). «Зомби-пинг» ЗАКРЫТ с #164: пульс = существование окна
  (`background.js:361` + `windows.onRemoved`), бэкенд реконсилит (`campaign.py:261`,
  18/18 тестов) — пункт «Сломано» от 09-07 был протухшим, удалён.

История слоёв: #98 (пульс = кампания, не расширение) → #164 (пульс = окно) → #171 (гейт
старта/режима fail-closed) → #174/#177/#178 (Indeed DOM + рубежи + failover) → #182/#183.

- **#186 (1.8.4)**: троттлинг — механизм закрыт. Зверь был не в значениях пауз (#154 их уже
  резал), а в часах: перекрытое окно = hidden → таймеры к 1 с, цепочки через 5 мин → 1/мин.
  `sleep()` теперь гонит SW-таймер (новый `SLEEP`, кап 30 с) против локального; выигрывает
  честный. Умирающий SW НЕ обнуляет паузу (только реальный `{ok}` завершает раньше локального
  — фикстура 3); сироты/молчание = ровно старое поведение. 4 фикстуры `tests/sw-sleep.test.js`.

## Сломано / не доделано

- **Троттлинг: механизм shipped (#186), ЗАМЕР НЕ СНЯТ.** Цель <90 с/форма не заявлена — по
  правилу этой цепочки её заявляет только живой прогон: таймстампы форм в activity_log,
  счётчик растёт, tap↔auto цел. Мерить на следующем Start Игоря (1.8.4 требует OFF/ON).
- Дрейф локального счётчика при сбросе storage (до 15 лишних; задокументировано
  `background.js:241`). Манифест просит права убранных платформ — чистить сквозной уборкой.

## Следующий шаг

Снять замер троттлинга с живого прогона (после OFF/ON на 1.8.4): время формы по activity_log
до/после, счётчик подач растёт. <90 с подтвердился — вычеркнуть пункт выше; нет — профилировать,
что ещё ест время (уже не таймеры).
