# campaign-runtime

Обновлено: 2026-09-13 · ветка: main · ext в проде: **1.8.5** (в CWS живёт 1.8.3)

## Состояние

Цикл: скрейп → фильтры → fit-гейт → walk по платформам → подача. Окно автоматизации **видимое**
(политика CWS), превью через CDP. Auto и Tap делят один движок; капы слоёные — серверная правда
`app/db/subscriptions.py`, клиентский ban-гейт с SAFE-дефолтом 20, на 429 стоп (#95).

**Порядок стадий (с 09-13, #189)**: Indeed → ZipRecruiter → ATS-пул, пул ВСЕГДА последний.
Живёт в двух чистых функциях `background.js`: `pickAtsOpener` (на чём прогон открывается) и
`pickNextStage` (куда передаёт ход). Пул достижим только через `PLATFORM_EXHAUSTED` — тем же
путём, что любая доска, поэтому «конец прогона» существует в одном месте. Переключаться можно
только на ВЫБРАННЫЕ юзером платформы. Фикстуры: `tests/platform-order.test.js` (14, включая
проверку, что цепочка передач завершается и не зацикливается).

**Очередь ATS = текущий поиск, не архив**: `GET /jobs/ats-queue` режет пул общим
`on_search_filter()` — тем же, что колода тапа. Отката к нефильтрованному пулу при ошибке нет.

Паузы-с-передачей-человеку — по одной схеме (строка в окне + лента + `DETECTION_TRIPPED` +
авто-возврат + стоп через 2 ч): капча (#72), логин-стена, стена согласия (#182). Капча в
pool-режиме СКИПАЕТ пик, стена согласия ПАУЗИТ и там (аккаунт-широкая); Accept не жмём.

Терминальная строка прогона несёт `metadata.outcome` (`completed` / `stopped_by_user`).
Отсутствие outcome = прогон оборвался (закрытый ноут, убитое окно) — дашборд читает именно
отсутствие, не текст строки.

## Последний заход (09-13)

Разбор пустого прогона Игоря: Start 06:29:57 → «ATS pool complete» 06:32:23. **2 м 26 с,
0 заявок, Indeed и ZR не искались** при всех шести выбранных платформах. Три дефекта:
- **Очередь читала архив.** `buildAtsQueue` брала голый `GET /jobs`; пул INSERT-only, поэтому
  прогон шёл по 15 инженерным вакансиям Oura/Braze, собранным под старые `ai engineer`, при
  профиле `event manager`. Судья зарубил их 2–12 при баре 35 — **каждый скип был верным**,
  поэтому три дня отчётов читались как «фит-скипы честные» и никто не смотрел на ВХОД.
  Колоду тапа от этого чинили 09-08, auto-ветку — нет.
- **Greenhouse съедал прогон**: `atsTarget` брал пул, если greenhouse просто ПРИСУТСТВОВАЛ
  в списке, доска не открывалась никогда.
- **«Пул пройден» = конец кампании**, а не передача хода.

Замер по живым данным после фикса: 281 применимая greenhouse-строка → **39** переживают
текущий поиск (маркетинг/ивенты вместо инженерии).

## Сломано / не доделано

- **Троттлинг: механизм shipped (#186), ЗАМЕР НЕ СНЯТ.** Цель <90 с/форма заявляет только
  живой прогон: таймстампы форм в activity_log, счётчик растёт, tap↔auto цел.
- **Новый порядок стадий живьём не прогонялся.** Юнитами закрыт, но передача Indeed → ZR → пул
  в реальном окне ещё не наблюдалась ни разу.
- Дрейф локального счётчика при сбросе storage (`background.js:241`); манифест просит права
  убранных платформ.

## Следующий шаг

Живой прогон на 1.8.5 (нужен полный OFF/ON — менялся manifest): проверить, что прогон
открывается на Indeed, а не на пуле; снять замер троттлинга по таймстампам форм; убедиться,
что строки в `applications` появляются.
