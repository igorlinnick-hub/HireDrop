# bug-swarm

Обновлено: 2026-09-25 · ветка: main (PR #241 #243 #244 #246 + райдер в #245)

## Состояние

Рой агентов — рабочая механика проекта, а не эксперимент. Закреплена так, что её видит
ЛЮБАЯ сессия: скил `../.claude/skills/swarm/SKILL.md` (когда предлагать, инварианты в промпт,
правила лейнов) + два шаблона `../.claude/workflows/`: `bug-hunt.js` (finder'ы по зонам →
дедуп → топ-N под 2 скептика) и `fix-conveyor.js` (fixer → 2 adversarial-ревью → repair).
Цикл не сокращается: последний шаг — независимый прогон тестов оркестратором, коммит явным
списком файлов, PR с телом «баг → дифф → чем проверен».

Что рой даёт сверх одиночной сессии — проверено делом, а не верой: два скептика опровергли
находку про `data/resume.pdf` (git-доказательство: файл в `.gitignore`, в деплой не едет) и
завернули фикс слаг-дедупа, который склеил бы разные вакансии Taleo `?job=111` / `?job=222`.
Своих ошибок одиночная сессия так не ловит — некому.

## Последний заход

- **Поиск (22 агента, run `wf_6d6ef69f-1f3`)**: 34 сырых → 6 CONFIRMED / 2 REFUTED /
  25 неверифицированных. Отчёт целиком — `docs/reviews/2026-09-19-bug-swarm.md`.
- **Все 6 CONFIRMED закрыты**: #244 (ложное «Applied»), #246 (замирание очереди + radio без
  `name`), #241 (подпись поиска + слаг-дедуп), #243 (гео-отбор слотов), удалён
  `POST /tools/cover-letter-template`; второй IDOR (`/tools/email-check`) оказался УЖЕ
  закрыт в #210 — конвейер доказал и не тронул.
- **Живой прогон 1.8.18 подтвердил фиксы делом** (68 мин, 8 подач, 0 ложных «Applied»):
  Suno/Twilio/DoorDash — валидация отказала → хендбэк, в `applications` ноль строк.

## Сломано / не доделано

- **25 неверифицированных гипотез** из отчёта никто не проверял — это НЕ баги, а кандидаты.
  Самые дорогие: Stripe-webhook помечает event обработанным ДО обработки, двойной checkout
  создаёт второго Stripe-customer, гонка дневного капа, salary-фильтр не применён к
  extension-harvest пути.
- **Stop fail-open — из гипотезы стал фактом**: 09-24 драйвер кликнул Stop в 20:57, движок
  работал до 22:00. Кандидат №1 следующего конвейера (`ping.js` ~109, канал без ack).
- **Артефакты роя живут в сессионной папке**: `~/.claude/projects/-Users-igorlinnik-Code-JobFlow/
  <session-id>/subagents/workflows/<runId>/journal.jsonl` — по строке на агента с полным
  возвратом (голоса скептиков, диффы). Читать можно всегда; `resumeFromRunId` работает
  ТОЛЬКО внутри той же сессии, из новой — перезапуск шаблона.
- Рой читает код, не браузер: DOM, капчи, реальный сабмит доказывает только живой прогон.

## Следующий шаг

Прогнать `fix-conveyor` по Stop fail-open (`ping.js` — ack на `HIREDROP_STOP_CAMPAIGN`) —
единственная находка, уже подтверждённая живьём, и она ломает управление кампанией.
