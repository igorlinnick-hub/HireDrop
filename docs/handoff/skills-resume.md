# skills-resume — второе резюме + диал дефолта

Обновлено: 2026-09-19 · ветка: main (#199/#163, #202/#166, #203/#170 — все смержены)

## Состояние
Гибридный skills-first формат (НЕ функциональное резюме — отвергнуто: рекрутеры/ATS
читают его как сокрытие пробелов): сгруппированные скилы ведут страницу, каждая
работа = 1–2 строки + «skills gained». `modules/skills_resume.py` реиспользует
хелперы `ats_pdf_generator` (один Claude-вызов структурирует → PDF+DOCX).
Endpoints: `POST /profile/resume/skills/generate` (тот же атомарный AI-квота-клейм,
что у ats/generate), `GET .../skills/url|docx-url`, `POST /profile/resume/default`.
Миграция `add_skills_resume.sql` ПРИМЕНЕНА к проду и проверена через PostgREST
(skills_resume_url, skill_groups jsonb, default_resume + CHECK).

**Диал — одна власть:** `profiles.default_resume` ('original'|'ats'|'skills');
`ats_approved` — только legacy-fallback при NULL (урок fit-mode: две власти = баг).
`best_signed_url` в apply-пути: per-job tailored → диал → legacy → оригинал;
пропавший сгенерированный файл падает на оригинал, не 404-ит подачу.
Website: ResumeATSPanel — блок Skills-First Version + трёхпозиционный дефолт,
все «use X» пишут диал + best-effort sync legacy approve/decline.

**Скилы пишет ЧЕЛОВЕК, мы корректоры (09-19, #203/#170).** Разворот решения Игоря:
не ИИ извлекает скилы из резюме, а кандидат перечисляет их сам. Промпт — явно
корректорский: исправить орфографию/грамматику/регистр, СОХРАНИТЬ формулировку,
не сливать/не дробить/не терять, НИЧЕГО не добавлять от себя; резюме = источник
скилов, только если человек не написал ни одного. Планка `MIN_SKILLS=10`;
`count_skill_items()` (разделители , ; \n • · | и «and») живёт в бэкенде и
покрыт тестами — витрина считает по тому же правилу, счётчик «7 / 10» не разойдётся
с API. `/profile/skills/describe` отдаёт skill_count / min_skills / meets_minimum,
но сохранение НИКОГДА не блокируется счётчиком (недописанный список должен пережить
закрытие вкладки). UI: раскрытый рабочий пример (инструменты поимённо, годы, софт-
скилы через «что реально было»), Save for later всегда, Save & Generate — от 10.
`profiles.skills_description` хранит текст, подмешивается в структурирование, переживает
регенерацию. Шаг «List your skills» — в `ChecklistCard.tsx` (чеклист переехал в рельсу
дашборда, #168), done = skill_groups ИЛИ skills_description.

## Последний заход
- Промпт переписан с авторского на корректорский; MIN_SKILLS + count_skill_items с тестами.
- UI: рабочий пример, счётчик 10, копирайт «мы не добавляем скилы за тебя».
- Заодно: main лежал красным по `ruff check`/`format` (чужой коммит 77e2fc0) — CI гоняет
  обе проверки, поэтому починил S110 в admin.py (проглоченное исключение читалось как
  «ошибок нет») и прогнал форматтер по admin.py + affiliate.py. Поведение не менялось.
- ⚠️ Грабли: `git add -A` втянул чужой untracked `docs/reviews/…` — вынул перед пушем.
  В общем репо коммитить только свои пути.

## Сломано / не доделано
- **Живой генерации ещё не было ни разу** — кнопку никто не нажимал на настоящем
  резюме; корректорский промпт = гипотеза до первого PDF. Смотреть в первом PDF:
  все ли скилы Игоря дошли, не переписаны ли формулировки, 3–6 ли групп.
- skill_groups показываются, но не редактируются вручную (только регенерацией).
- Дефолт-диал в проде, но подача с `default_resume='skills'` живьём не наблюдалась.

## Следующий шаг
Игорь перечисляет ≥10 своих скилов (Settings → Resume & ATS → List your skills) и
жмёт Save & Generate — смотрим первый PDF глазами и правим промпт под увиденное.
