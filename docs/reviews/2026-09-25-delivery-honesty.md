# Рой: честность доставки (lane «письмо/форма») — 2026-09-25

Инвариант лейна: **всё, что продукт записал в БД или показал юзеру как сделанное, должно
быть подтверждено наблюдаемым эффектом в форме работодателя.** Повод — доказанный случай
09-25: письмо писалось на каждой заявке и попадало в форму 1 раз из 359 шагов.

## ⚠️ Прогон НЕ полный — читать с этой поправкой

Рой (`bug-hunt`, run `wf_7ebbded2-f09`, 14 агентов) упёрся в лимит сессии:

| Фаза | Итог |
|---|---|
| Искатели | 1 из 4 дошёл (`screener-stick`). `resume-attach`, `profile-orphans`, `delivery-claims` — **не отработали** |
| Скептики | **0 из 10** дошли |

Поэтому машинный вердикт «0 confirmed / 5 refuted» — **артефакт**: без голосов скептиков
находка помечается опровергнутой. Настоящий статус всех находок — «не проверено роем».
Ниже — то, что я проверил **сам, чтением кода и БД**, и то, что осталось кандидатами.

## ✅ Проверено лично и ЗАКРЫТО (PR: fix/delivery-honesty)

### 1. `content.js` — «письмо вставлено ✓» писалось без чтения поля обратно
Мой же фикс #248 повторял ловимый класс: после `quickSet(el, letter)` шла durable-строка
«cover letter filled ✓» и письмо уходило в `applications`, **не прочитав поле**. `quickSet`
возвращает `true` за «присвоил и разослал события», а не за «значение осталось»: компонент
со своим состоянием может откатить присвоение на следующем рендере. Итог был бы ровно тот,
против которого лейн и существует — доказательство отправки без отправки.
**Фикс:** читаем `el.value` после вставки; пусто → честное «did NOT stick», письмо НЕ
записывается; в БД уходит текст, который поле реально держит.

### 2. `content.js` — работодателю уходила выдуманная зарплата «65000»
Ветка зарплатных вопросов делала `profile.desired_salary || "65000"`. Проверка:
`desired_salary` **не существует нигде** — ни в таблице `profiles` (там только
`salary_min` / `salary_max` / `salary_listed_only`, и `get_profile()` их даже не отдаёт), ни
в схемах, ни в UI. То есть **каждый** юзер на любом уровне сообщал работодателю ожидание
65 000, которого он не заявлял и которое ему нигде не показывают.
**Фикс:** не выдумывать. `salary_min` — не замена: юзер задал её, чтобы ФИЛЬТРОВАТЬ
вакансии, и переиспользовать её как заявленное ожидание = сделать фильтр второй властью
над тем, на что он не отвечал. Нет источника → поле остаётся пустым, шаг не проходит
валидацию и вопрос уходит человеку по циклу возврата (#230) — штатный ответ на «не знаю».

## 🕯 Кандидаты (проверял только искатель; скептиков не было — НЕ чинить без перепроверки)

1. **`content.js:3266` — мёртвая проверка в `fillComboboxes`.** `combo.dataset.hdDone = "1"`
   ставится строкой ВЫШЕ, а `isUnfilled()` первым делом возвращает `false` при `hdDone` —
   значит `if (isUnfilled(combo)) …hdSkip` не исполняется никогда. Следствие: `filled++`
   считает КЛИК, не подтверждённый ответ, и лог «answered combo×N» — попытки. Я прочитал
   обе функции, логика подтверждается, но это общий филлер (лейн `ats-form-filler`, там
   сейчас другая сессия) — не трогал.
2. **`content.js:2840`** — `fillTextQuestions` идёт по снимку DOM, взятому ДОмногосекундных
   ИИ-запросов; ре-рендер отцепляет оставшиеся поля, запись уходит в осиротевшие узлы и
   всё равно считается ответом. (У соседнего `fillComboboxes` ровно от этого есть
   пере-запрос каждый проход — здесь его нет.)
3. **`content.js:2994`** — возврат `false` от `fillReactSelect` отбрасывается: typeahead
   без меню остаётся пустым, но попадает в счётчик отвеченных.
4. **`content.js:3759`** — `phase3` помечает имя/почту/телефон как `filled`, даже когда
   `typeValue` ничего не сделал (пустое значение в профиле): прогресс фиктивный, а
   stall-guard после этого не срабатывает.

## Что стоит запустить, когда лимит отпустит

Три зоны не отработали вовсе: **resume-attach** (то ли резюме прикрепилось, что записано),
**profile-orphans**, **delivery-claims**. Перезапуск: тот же шаблон, `resumeFromRunId`
работает только внутри той сессии — из новой запускать заново.
