# Bug-swarm отчёт — 2026-09-19

Прогон: 22 агента (6 finder-зон + adversarial verify 2 скептика/находку).
Сырых находок: 34, после дедупа: 33, проверено: 8.
Полный JSON с голосами скептиков: см. git-историю этого файла / прогон wf_6d6ef69f-1f3.

## CONFIRMED (2/2 скептика подтвердили)

### [CRITICAL] `jobflow/chrome-extension/content.js:618`

waitForSubmissionConfirmation matches generic success phrases against the WHOLE page body with no pre-click baseline, so pre-existing job-description text instantly 'verifies' a submit that was actually blocked

**Сценарий:** Greenhouse/Lever/Ashby render the job description and the apply form on the SAME page, and postings very commonly contain phrases from SUCCESS_TEXTS ('thank you for your interest', "we'll be in touch", 'application received' — lines 593-600). phase_ats clicks submit (line 4220) and calls waitForSubmissionConfirmation (line 4222); on its FIRST loop iteration the function scans document.body.textContent (lines 618-623) with no check that the phrase is new or that the URL changed. If a required field was left unfilled and the submit was refused by validation, the description text still matches, so result.verified=true is returned immediately — which makes the validation-blocked honesty check at lines 4228-4243 (gated on !result.verified) unreachable. The job is recorded as '✅ Applied' with verified:true, the receipt says verified, dedup keys prevent ever retrying it, and the employer never received anything. URL hints are correctly guarded by url !== startUrl (line 611); the text channel has no such guard.

```
const bodyText = (document.body.textContent || "").toLowerCase();
for (const phrase of SUCCESS_TEXTS) {
  if (bodyText.includes(phrase)) {
    return { verified: true, signal: `text:${phrase.slice(0, 30)}` };
  }
}  // no snapshot of pre-click body text; SUCCESS_TEXTS includes "thank you for your interest", "we'll be in touch"
```

### [CRITICAL] `jobflow/chrome-extension/content.js:3268`

detectSilentSubmission reads the job page BEHIND the modal during a step transition and can record a half-filled application as verified-applied, then abandon the open form

**Сценарий:** After every non-submit Continue click, phase3 calls detectSilentSubmission (line 3656). Its mid-flow guard (line 3266) only checks whether some visible dialog holds input/textarea/select — but ZipRecruiter briefly renders a bare [Close]-only shell between steps (documented at lines 3222-3224), and Indeed SmartApply steps are full PAGES with no dialog at all, so the guard passes mid-form. It then runs jobLooksApplied() over the right-pane/document and waitForSubmissionConfirmation(1200) over document.body — both of which scan the job description still visible behind/around the form. A description containing "we'll be in touch" or "application received" (SUCCESS_TEXTS, lines 593-598) returns a signal → recordSubmittedApplication() fires (line 3659): appliedUrls/appliedJobKeys added, local count incremented, APPLICATION_SAVED sent with verified:true, and goBackToJobList() abandons the half-filled form. The user sees a verified application that was never submitted, and dedup guarantees it is never retried.

```
if (visibleApplyDialogs().some((d) => d.querySelector("input, textarea, select"))) return null;
if (jobLooksApplied()) return "applied-badge";
const conf = await waitForSubmissionConfirmation(1200);
if (conf.verified) return conf.signal;  // scans full body text of the page behind the modal
```

### [HIGH] `jobflow/app/routers/tools.py:321`

POST /tools/cover-letter-template lets any authenticated user overwrite the GLOBAL cover-letter template served to all other users

**Сценарий:** User A (any account, no admin check) POSTs {"template": "I was fired for stealing. {name}"} to /tools/cover-letter-template. The write goes to the single shared file templates/cover_letter.txt with no per-user path. Later, during any Anthropic outage/error (or missing key), user B's POST /tools/cover-letter or /applications flow hits fallback_template(), which renders A's text with B's name substituted — B submits A's arbitrary content as their own cover letter on a real job application. Cross-tenant stored-content injection; no user_id scoping at all on the write or the read.

```
@router.post("/tools/cover-letter-template")
def save_letter_template(req: TemplateRequest, user=Depends(get_current_user)):
    os.makedirs(TEMPLATES_DIR, exist_ok=True)
    with open(os.path.join(TEMPLATES_DIR, "cover_letter.txt"), "w") as f:
        f.write(req.template)
    return {"saved": True}
-- and modules/ai_cover_letter.py:60 reads the same file in fallback_template(), which generate_cover_letter() returns whenever ANTHROPIC_API_KEY is unset OR the Anthropic call raises (ai_cover_letter.py:146-148).
```

### [HIGH] `jobflow/app/routers/tools.py:329`

GET /tools/email-check exposes the shared ops inbox (all users' recruiter responses) to any authenticated user

**Сценарий:** With EMAIL_ADDRESS/EMAIL_PASSWORD set in Railway env, ANY logged-in user (or anyone with an hd_ extension key) calls GET /api/v1/tools/email-check and receives the raw subjects, senders and inferred company names of interview/rejection emails belonging to ALL users that land in the shared inbox — the same data class whose exposure was already classified as a cross-tenant leak when /admin/process-emails was deleted. Also does blocking imaplib IO on the request worker.

```
@router.get("/tools/email-check")
def email_check(user=Depends(get_current_user)):
    from config import EMAIL_ADDRESS, EMAIL_PASSWORD
    ...
    responses = check_email_responses()
    return {"configured": True, "count": len(responses), "emails": responses}
-- check_email_responses() (modules/email_parser.py:78) logs into the ONE configured EMAIL_ADDRESS inbox and returns subject/sender/date/company for every unseen matching email, with no per-user filtering. app/routers/email_processor.py:13-19 documents that the equivalent /admin/process-emails endpoint was REMOVED 2026-06-29 as a cross-tenant leak — this sibling read path was left behind.
```

### [HIGH] `jobflow/chrome-extension/content.js:3549`

Two early returns in _phase3_fillForm (resume fail-closed guard and review-mode skip) exit without advancing the walk — the campaign silently dead-stops on the open form

**Сценарий:** On the final submit step, if a resume input is present but empty and 'resume.pdf' is not on the page, the guard logs 'Skipped (no resume attached)' and does a bare `return;` (lines 3545-3550) — no skipToNextJob(), no handBackJob(), no ATS_JOB_DONE. Same for review-mode skip at lines 3524-3527. The form/modal stays open, detectPhase() keeps returning 'form', lastPhase is already 'form', so the MutationObserver (line 5010: only re-runs on a phase CHANGE) never re-triggers a phase. In pool mode the queue head is never advanced (compare phase_ats, which sends ATS_JOB_DONE on its review-skip at line 4180 and hands back on its resume guard at line 4190). Result: a 'running' campaign that applies to nothing until the server-side stall watch notices — the exact silent-freeze shape this file's own comments (live 09-06) describe.

```
const rz = findResumeInput();
if (rz && !rz.files?.length && !document.body.textContent.includes("resume.pdf")) {
  logBackend(`⏭️ Skipped (no resume attached): ...`, "error");
  return;  // nothing advances the walk — contrast phase_ats which calls handBackJob/ATS_JOB_DONE
}
```

### [HIGH] `jobflow/chrome-extension/content.js:2417`

fillRadioQuestions crashes with a TypeError on any radio input that has no name attribute, dropping the entire job

**Сценарий:** For a radio without a name attribute, r.name === "" and the group lookup `document.querySelectorAll('input[name=""]')` (line 2406) matches NOTHING (attribute selector requires the attribute to be present), so group = []. Line 2417 then executes `group[0].closest(...)` → TypeError: Cannot read properties of undefined. In phase3 this is caught by the phase3_fillForm wrapper (line 3305) which logs 'Form filler crashed' and calls skipToNextJob() — the job is dropped every single time this form is opened (deterministic: the same form always crashes, so that posting can never be auto-applied). In phase_ats (call at line 4122) the throw propagates to _runPhaseInner's catch (line 4984) and the pool head is skipped and flipped out of approved — a user's explicit swipe-approved pick burned by one unnamed radio in a React form.

```
const group = Array.from(document.querySelectorAll(`input[name="${r.name}"]`));  // r.name==="" → []
if (group.some((o) => o.checked)) continue;
...
const groupLabel = getFieldLabel(group[0].closest("fieldset, [role='radiogroup'], [role='group']") || group[0]);  // group[0] is undefined
```

## REFUTED (опровергнуто — НЕ чинить без перепроверки)

### `jobflow/chrome-extension/background.js:1611` — START_CAMPAIGN drops the dashboard's platform_mode field, so platformFailover is always true and single-platform runs fail over to boards the user never picked

- The evidence chain is mechanically accurate — background.js:1330-1341 does rebuild `filters` without copying `platform_mode`, so line 1611 always computes `platformFailover: true` and the `ex.platformFailover === false` branch at line 1717 is dead. BUT the claimed critical impact ("real applications submitted on boards the user never picked") is unreachable, because there is a second, independent consent guard on the exact path: every failover target comes from `pickNextStage(tried, (ex.campaignFilters||{}).platforms, conns)` (background.js:714-723, called at 1729), and that function only returns platforms with `sel.includes(p)` — its own comment says it exists to prevent precisely this consent breach. In a single-pick run, `campaignFilters.platforms` cannot contain any other walkable board: QuickActions.tsx:399-405 builds `effPlatforms = [the one pick, ...platforms.filter(x => !AUTO_APPLY_IDS.includes(x))]`, and AUTO_APPLY_IDS (lib/constants.ts) includes indeed, ziprecruiter, greenhouse, lever, ashby — i.e. every board pickNextStage can ever select (indeed/ziprecruiter at 718-719, greenhouse at 722) is excluded from the tail. The server echoes the posted list without adding platforms (app/routers/campaign.py:204 `run_platforms = list(req.platforms)`; it only removes lever). So on exhaustion of the single picked board, pickNextStage returns null and the run completes — no cross-platform submission, no #180 violation. What actually remains is a minor cosmetic defect, not the claimed critical one: because the 1717 branch never fires, a single-pick user sees the generic "no other platform left — run complete" message instead of the tailored "you picked X only... choose All connected" explanation, and the platform_mode field is silently dropped (worth fixing by copying it in the 1330-1341 merge), but no application is ever submitted on an unchosen platform.

- The code facts are accurate — background.js:1330-1341 does drop platform_mode from the filter merge, so platformFailover at line 1611 is always true and the consent-stop branch at line 1717 is unreachable from the dashboard path. But the claimed critical harm (applications submitted on platforms the user never picked) is NOT reachable: pickNextStage (background.js:714-723) independently enforces the same consent boundary by only selecting failover targets that are in campaignFilters.platforms. In single-pick mode the dashboard sends platforms=[picked board + non-auto-apply discovery sources only] (QuickActions.tsx:399-405 filters out all other auto-apply boards), the backend never adds platforms (campaign.py:204-209 only removes lever), and discovery sources (google/remoteok/linkedin) are not pickNextStage candidates (only indeed/ziprecruiter/greenhouse). So on exhaustion of the single picked board, pickNextStage returns null and the run stops cleanly ("run complete", lines 1730-1741). The only real effect of the dropped field is cosmetic: the user sees the generic completion message instead of the tailored "you picked X only" message, and lines 1714-1726 are dead code — a minor messaging/dead-code issue, not the critical #180 consent-breach defect claimed.

### `jobflow/modules/ai_cover_letter.py:47` — load_resume_text falls back to the repo-shipped data/resume.pdf (Igor's real resume) for any user whose resume is missing or fails to download

- The claim's core premise is false: data/resume.pdf is NOT checked into git. It is explicitly listed in jobflow/.gitignore (line 6), git ls-files shows only ats_watchlist.py and gh_form_schemas.jsonl under data/, and git log --all for the file is empty (never committed in any branch's history). Railway deploys from the GitHub repo (merge→deploy), so the file cannot exist in the production container; os.path.exists() at ai_cover_letter.py:48 is False there and load_resume_text returns "" for any user whose resume is missing or fails to download. All cited callers handle "" safely: _lazy_tailor_for_job (profile.py:689) returns early on falsy resume_text, generate_cover_letter renders "Not provided.", score_jobs_batch gets an empty string. The file exists only on Igor's local single-user dev machine (mode 600, code comment "Local fallback (legacy / dev)"), where the fallback resume belongs to the only user — no cross-tenant PII leak is reachable.

- The claim's core premise is factually wrong: jobflow/data/resume.pdf is NOT checked into git. It is explicitly listed in .gitignore (line 6, verified via `git check-ignore -v`), absent from `git ls-files`, and has zero commit history (`git log --all -- data/resume.pdf` is empty). The file exists only on Igor's local dev machine. Railway deploys from the git repo (merge→deploy), so the production container never contains the file; `os.path.exists(local_path)` is False in prod and `load_resume_text` returns "". Callers then behave safely: generate_cover_letter substitutes "Not provided." and the lazy-tailor path (profile.py) aborts on falsy resume_text. The fallback — commented "Local fallback (legacy / dev)" — can only fire on the founder's own machine serving his own resume, so the cross-tenant PII-leak scenario is unreachable. The cited evidence ("git ls-files data shows it") does not match reality: that command returns only ats_watchlist.py and gh_form_schemas.jsonl.

## UNVERIFIED (ниже топ-8, скептиками НЕ проверялись — доверять как гипотезам)

- **[high]** `jobflow/app/routers/activity.py:49` (campaign-state) — The extension's own campaign-death announcement refreshes last_ping_at, so its campaign_running=false ping in the same tick cannot reap the flag — a guaranteed ~10-minute zombie after the automation window dies
  - Laptop lid closes mid-run and the campaign window dies. On the next ext-ping alarm, background.js sendExtensionPing (chrome-extension/background.js:372-379) sets local campaignRunning=false and awaits addToActivityLog('Campaign window is gone...') BEFORE the ping fetch. addToActivityLog posts /activity with phase 'extension' (background.js:572), and activity.py:46-49 treats ANY phase=='extension' line as a campaign heartbeat while the DB flag is still up — including this line that announces the 
- **[high]** `jobflow/app/routers/campaign.py:100` (campaign-state) — /campaign/queue slices the daily budget over platforms the executor will discard: TAP_APPLY_PLATFORMS includes 'indeed' but the extension's default pool set excludes it, so approved Greenhouse/Ashby jobs get starved and a tap Start is refused with 'no_approved_jobs'
  - A tap-mode user swipes Approve on 12 Indeed cards (newest) and 5 Greenhouse cards (older); daily budget_left is 10. campaign_queue builds `waiting` from get_jobs (ordered date_found DESC, app/db/jobs.py:15), keeps indeed rows because TAP_APPLY_PLATFORMS = ('greenhouse','lever','indeed','ashby') (app/routers/jobs.py:87), applies the per-platform cap, then hard-slices `capped[:budget_left]` — the 10 returned rows are all Indeed. In the extension, buildApprovedAtsQueue (chrome-extension/background.
- **[high]** `jobflow/app/routers/billing.py:193` (money-caps) — Webhook marks the Stripe event processed BEFORE handling it and always acks, so any transient failure permanently consumes the event — lost tier grants and permanently lost affiliate commissions on Stripe's retry
  - invoice.paid arrives -> mark_event_processed inserts the event id (returns True) -> billing_db.grant() hits a transient Supabase 5xx and raises -> outer except logs and returns 200. Stripe retries the same event id -> mark_event_processed returns False -> skipped as duplicate. The tier expiry is never extended (paid user fail-closes to free when subscription_expires_at passes) and accrue_from_invoice never ran for that invoice — no later event carries it, so the affiliate commission is lost fore
- **[high]** `jobflow/app/routers/billing.py:213` (money-caps) — First invoice.paid can arrive before checkout.session.completed links the customer; the unresolved event is still consumed by the dedup table, so the first invoice's affiliate commission is silently and permanently dropped
  - Referred user pays for the first time. Stripe emits checkout.session.completed and invoice.paid nearly simultaneously with no ordering guarantee. invoice.paid is delivered first -> stripe_customer_id not yet on the profile -> find_user_by_customer returns None -> handler acks with the event already marked processed. Stripe's redelivery is rejected as duplicate. accrue_from_invoice (the ONLY accrual path, by design driven by invoice.paid) never runs for that invoice: on the weekly plan the affili
- **[high]** `jobflow/app/routers/billing.py:62` (money-caps) — Checkout has no already-subscribed guard and each checkout creates a NEW Stripe customer whose id overwrites the profile, so a second checkout double-bills the user and orphans the first subscription beyond the portal's (click-to-cancel) reach
  - User subscribes Weekly ($12/wk, Stripe customer A). Later opens the pricing page and clicks Monthly -> POST /billing/checkout succeeds -> new customer B, second active subscription. checkout.session.completed overwrites stripe_customer_id A->B. Both subscriptions now bill in parallel. /billing/portal shows only customer B, so the Weekly sub under A has no in-product cancel path (breaking the module's own FTC click-to-cancel promise); customer.subscription.deleted for A later resolves to no user 
- **[high]** `jobflow/app/routers/jobs.py:535` (pool-pipeline) — The extension-harvest ingest path never applies the user's salary filter, and no read/submit path (deck, ats-queue) re-applies it, so out-of-range Indeed/ZR jobs get auto-applied.
  - User sets salary_min=80000 (or salary_listed_only). Extension harvests an Indeed card whose snippet says "$18/hr" and POSTs /jobs/ingest. ingest_jobs builds rows and calls save_jobs_bulk with NO filter_by_salary call (unlike find_jobs line 267 and _run_ats_discovery line 344 — grep confirms only those two callers). The row lands status=new, platform=indeed. GET /jobs/ats-queue (indeed is zero-touch per captcha_profile) and GET /jobs/deck both pass it: on_search_filter (lines 90-117) checks only 
- **[high]** `jobflow/modules/platforms/craigslist.py:125` (pool-pipeline) — Uncaught concurrent.futures.TimeoutError from as_completed(timeout=20) escapes CraigslistPlatform.scrape and 500s the whole /jobs/find request, discarding every other platform's already-scraped jobs.
  - A profile has craigslist enabled (still in PLATFORMS registry, no unavailable_reason). 20 cities are fetched on 8 workers with a 10s per-request timeout: if cities respond slowly (~10s each — common when craigslist throttles scrapers), completing all futures takes ceil(20/8)*10 ≈ 30s > the 20s as_completed timeout. The TimeoutError is raised at the for-loop's next() call — OUTSIDE the try that only wraps future.result() — and propagates out of scrape(). In find_jobs (app/routers/jobs.py:230-239)
- **[high]** `jobflow-website/components/dashboard/QuickActions.tsx:179` (ext-protocol) — saveMode flips the extension's live reviewMode via postMessage before the profile write, and on DB failure reverts only the UI — extension, profile, and UI end up in three different submit modes
  - A campaign is running with reviewMode=true (user picked Tap mid-run). The user clicks Auto: SET_REVIEW on:false immediately clears chrome.storage.reviewMode; the profiles UPDATE then fails (network blip / expired session). UI reverts to showing Tap, profile still says tap — but the live run resumes AUTO-SUBMITTING applications the user believes they will review. The reverse direction stalls an Auto run into 30-minute-per-job review timeouts while the UI says Auto. This is the documented three-pl
- **[medium]** `jobflow/chrome-extension/content.js:3594` (apply-filler) — Indeed/ZR phase3 submit path lacks the validation-blocked detection phase_ats has: a submit refused by form validation is still recorded as applied_unconfirmed with dedup keys, then the walk navigates away
  - phase3 pre-records the application before the click (lines 3558-3560, nav-safe by design), clicks submit, and on !result.verified (line 3587) saves status 'applied_unconfirmed', keeps the appliedUrls/appliedJobKeys dedup entries and the incremented count, and calls goBackToJobList() — WITHOUT checking whether the submit button is still connected, the URL unchanged, and required fields still empty ([aria-invalid="true"]). phase_ats got exactly this honesty fix (lines 4228-4243: subtractLocalAppli
- **[medium]** `jobflow/chrome-extension/content.js:2534` (apply-filler) — fillTextQuestions answers ANY label containing 'date' with today's date — including 'Date of birth' and 'Start date' — and shadows the dedicated notice-period/start-date branch
  - The branch `} else if (label.includes("date")) { value = today; }` (line 2534) runs before every later branch. Input: a screener text field labeled 'Date of birth' (common on age-gated jobs) → filled with today's date, i.e. a birth date of today silently submitted under the user's name (or a validation error that later becomes a hand-back with a confusing pre-filled wrong value). Input: 'Start date' / 'Earliest start date' → filled with today instead of reaching the notice-period branch at line 
- **[medium]** `jobflow/chrome-extension/background.js:1476` (campaign-state) — START_CAMPAIGN swallows a backend REFUSAL of /campaign/start (4xx) and launches the run anyway; the run is then killed <=60s later by should_run=false with the misleading line 'Stopped by the server', and every retry loops the same doomed run
  - A user with a disposable email presses Start. The extension's local pre-flight passes (it has no disposable-email check; /campaign/status GET at background.js:1359 succeeds because that endpoint has no such gate). `await apiPost("/campaign/start", filters)` at background.js:1476 gets a 403 detail='disposable_email' (app/routers/campaign.py:180-184), but the bare `catch {}` (1477-1479, commented 'Continue even if server is down') cannot tell a deliberate refusal from a network failure — the exten
- **[medium]** `jobflow/app/routers/campaign.py:348` (campaign-state) — GET /extension/ping reports the raw campaign flag via get_state() instead of the authoritative get_effective_state(), so a zombie campaign (flag up, heartbeat stale) is reported campaign_running=true on this surface
  - A campaign row has running=true but last_ping_at 30 minutes stale (browser gone; no /campaign/status poll has run yet to lazily reap it — the reap only happens inside get_effective_state, campaign.py db:154). A client calls GET /extension/ping (e.g. the dashboard DevPanel, jobflow-website/components/dashboard/DevPanel.tsx:80): line 348 computes `running = bool(campaign_db.get_state(user.id)["running"])` — the raw flag, no heartbeat check — and returns {online: false, campaign_running: true}: 'ex
- **[medium]** `jobflow/app/routers/applications.py:24` (money-caps) — Daily cap and the 40-app lifetime free-taste gate are read-then-act: concurrent /applications/save requests can race past both limits
  - User at 29/30 used (or 39/40 free): two /applications/save requests land concurrently (tap relay from the phone + the extension reporting, or a double-fired report). Both read used_today=29 (free_used=39), both pass check_can_apply, both insert into applications -> 31 apps on the day / 41 lifetime free apps; the per-platform MAX_PER_PLATFORM ban-safety rail (line 224) is raceable the same way. The free counter itself stays exact via RPC, but the gate was checked on the stale read.
- **[medium]** `jobflow/app/routers/billing.py:130` (money-caps) — A subscription on a price id not in the current env (e.g. a legacy pre-repricing Price — Stripe Prices are immutable, repricing created new ones) is silently never granted, so the customer keeps being charged while get_tier fail-closes them to free at period end
  - Pricing moved $29->$39 by creating a new Stripe Price (old subscriptions keep their old Price id on their items). Old subscriber renews: invoice.paid -> _grant_from_subscription -> tier_for_price(old_price_id) returns None -> return without touching subscription_expires_at. The stored expiry (last successful grant) passes -> get_tier returns "free" -> the user is capped at the free tier and hits the free-taste paywall while Stripe continues collecting their money every cycle. Same applies to any
- **[medium]** `jobflow/app/db/subscriptions.py:207` (money-caps) — Cap ENFORCEMENT counts 'today' by server/UTC date while the campaign status the extension paces by uses the client's local midnight, so a user straddling the UTC rollover can submit up to 2x the daily cap (and 2x the per-platform ban-safety rail) within one local day
  - Hawaii user (UTC-10): they submit 30 apps in the morning; at 14:00 local the UTC date rolls over -> count_today in check_can_apply resets to 0 -> the server allows 30 more the same local day (60 total), including up to 30 on ONE platform across the boundary (2 x MAX_PER_PLATFORM=15) — defeating the per-platform ban-safety cap the code calls the single source of truth; the dashboard simultaneously shows a different used_today than the 429 the save endpoint returns.
- **[medium]** `jobflow/modules/ai_job_scorer.py:94` (pool-pipeline) — Any scorer failure (API outage, bad JSON, rate limit) returns _default_score() with score=5, which is persisted as a genuine mid-band score and is never distinguishable or repaired.
  - Anthropic API is down during a discovery sweep or /jobs/ingest. score_job catches the exception and returns {score: 5, verdict: 'сомнительно'} for EVERY job; score_jobs_batch attaches it and save_jobs_bulk persists it. Consequences traced in code: (1) the deck and ats-queue sort by score desc (jobs.py:145,202), so failure-scored jobs rank ABOVE genuinely-scored 3-4 jobs and at the level of legitimately-capped description-less jobs (the prompt's 'score at most 5' rule uses the same number — outag
- **[medium]** `jobflow/app/routers/jobs.py:338` (pool-pipeline) — record_scrape("ats_boards", len(found)) now measures the post-exclude NEW-inventory count, so a saturated pool produces a legitimate zero-streak that the silent-zero watch reports as a dead scraper.
  - After #196, discover_ats is called with exclude=known (all links already in the user's pool) at line 332, and `found` contains only never-seen postings. Once a user's pool has walked out to the watchlist's full supply (the explicitly stated goal of the exclude change — 'the pool walks out to the watchlist's real supply in a few sweeps'), every subsequent sweep correctly returns found=[] even though all 200+ boards answered fine. ScrapeWatch.record (app/ops_watch.py:106-143) counts consecutive ze
- **[medium]** `jobflow/chrome-extension/ping.js:109` (ext-protocol) — Stop is fail-open: HIREDROP_STOP_CAMPAIGN has no ack and its context_invalidated catch swallows silently, so an orphaned bridge leaves the extension applying while the dashboard shows the campaign stopped
  - Extension is updated/reloaded while the dashboard tab stays open → ping.js in that tab is orphaned (chrome.runtime.sendMessage throws synchronously) while the automation window keeps running on chrome.storage.campaignRunning=true. User presses Stop: backend flips to stopped, the bridge relay dies silently, UI shows stopped — and the extension continues navigating and submitting real applications with nobody watching the 'stopped' campaign.
- **[medium]** `jobflow/app/routers/review.py:54` (ext-protocol) — The phone tap-review relay is dead on both ends — no code ever calls RELAY_REVIEW_PENDING/FETCH_REVIEW_DECISION or the /review endpoints — and its decision vocabulary ('approved'/'skipped') contradicts the storage bridge ('approve'/'skip')
  - A legacy-tap (reviewMode=true) run fills an application while the user's only open dashboard is on their phone: awaitReview publishes the card ONLY to chrome.storage on the desktop, the backend relay the router was built for never receives it, the phone shows nothing, and after 30 minutes every filled application is auto-skipped — the run silently produces zero submissions. Any future wiring that forwards the storage decision verbatim will also be rejected by the 400 at line 54 because the two s
- **[low]** `jobflow/app/routers/tools.py:117` (idor-auth) — GET /checklist reports resume=true for every user because the shared server file data/resume.pdf always exists
  - A brand-new user with no resume uploaded calls GET /api/v1/checklist. Because the container ships data/resume.pdf, has_resume evaluates True and the response returns resume:true and can return complete:true — the onboarding surface tells the user their resume step is done when nothing exists for their account, masking the exact gap that later makes their applications run on the fallback resume (finding above).
- **[low]** `jobflow/chrome-extension/background.js:1059` (campaign-state) — Self-stop paths (nativeWalkWatchdog two-strike stop, APPLICATION_SAVED 429 stop, free-limit stop) lower campaignRunning without detaching the CDP debugger or clearing campaignTabId/campaignWindowId — Chrome's 'HireDrop started debugging this browser' banner persists after the run ends
  - An auto run on Indeed goes silent; nativeWalkWatchdog reloads twice then stops the campaign (background.js:1059-1065): it sets campaignRunning=false and POSTs /campaign/stop, but unlike the STOP_CAMPAIGN handler (which calls detachDebugger(campaignTabId) at 1843 and nulls campaignTabId/campaignWindowId/currentJob/captchaWaiting at 1825-1833) it leaves the CDP debugger attached to the automation tab, leaves campaignTabId/campaignWindowId set, and never sends CAMPAIGN_STOPPED to the content script
- **[low]** `jobflow/chrome-extension/content.js:5133` (campaign-state) — The screenshot keep-alive interval fires every 300 ms (its own comment says 'every 2.5 s'), so every open tab on any matched job site — including the user's own Indeed/LinkedIn browsing tabs — wakes the MV3 service worker ~3x per second forever, the exact 'SW kept alive on old code' trap the codebase documents elsewhere
  - A user keeps a personal indeed.com tab open (content.js injects on all manifest-matched hosts, campaign or not). The interval at content.js:5131-5133 sends CAPTURE_SCREENSHOT every 300 ms — 3.3 messages/sec per tab — while the comment above it (5128-5130) documents 2.5 s. Each message wakes/keeps-alive the service worker (captureActiveAutomationTab returns early when campaignRunning is false, but the wake already happened). Consequence beyond CPU: the SW is pinned alive indefinitely on old code,
- **[low]** `jobflow/modules/platforms/remoteok.py:60` (pool-pipeline) — RemoteOK hardcodes job_type="full-time" for every posting, fabricating data the type filter then trusts — contradicting the codebase's own 'silence is not full-time' rule.
  - RemoteOK's scrape builds every job with "job_type": "full-time" regardless of the posting (a listing titled 'Designer (Contract)' gets full-time). detect_job_type is never consulted, unlike ats_boards._job (ats_boards.py:195, whose comment states 'None stays None: silence is not full-time'). Downstream, matches_job_type (modules/job_type.py:70-77) does exact equality: a user whose profile job_type='contract' has real RemoteOK contract postings filtered OUT of get_deck/get_ats_queue via on_search
- **[low]** `jobflow/app/db/jobs.py:172` (pool-pipeline) — all_links paginates with .range() but no .order(), so page boundaries are nondeterministic and links can be missed, silently punching holes in the discovery exclude set.
  - all_links pulls the link column in 1000-row pages via .range(start, start+999) with no order clause — PostgREST/Postgres gives no stable row order without ORDER BY, and rows physically move under concurrent writes. Concrete interleaving: _run_ats_discovery calls all_links while the user's extension is mid-campaign POSTing /jobs/ingest (or a tap swipe updates a row); between reading page 1 and page 2 an insert/update shifts an existing row across the page boundary, so its link lands in neither pa
- **[low]** `jobflow/chrome-extension/ping.js:210` (ext-protocol) — Two handlers for the same HIREDROP_SET_FLAG message type with conflicting allowlists and incompatible reply shapes
  - A caller using the newer flag/on form to enable the tap rollout flag — {type:"HIREDROP_SET_FLAG", flag:"tapNativePool", on:true} — is refused with {ok:false, reason:"not allowlisted"} even though tapNativePool IS allowlisted in the first handler under `key`; a message carrying both `key` and `flag` triggers both handlers and produces two contradictory HIREDROP_FLAG_SET replies with different field names, so any listener keying on the reply reads whichever arrives last.