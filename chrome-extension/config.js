// HireDrop Extension — single config file
// All URLs and constants live here, nowhere else

const CONFIG = {
  API_BASE: "https://web-production-db45.up.railway.app",
  API_V1: "/api/v1",
  DASHBOARD_URL: "https://hiredrop.io",
  CONNECT_PATH: "/extension/connect",
  CACHE_TTL_MS: 5 * 60 * 1000, // 5 minutes
};
