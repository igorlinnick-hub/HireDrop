# Dev/test only. Production image installs requirements.txt.

# Test runner
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=7.1.0
httpx>=0.27

# Lint + format
ruff==0.16.6  # pinned: CI installs this exact version — 0.15 vs 0.16 format drift broke the gate

# Type check
pyright>=1.1.380

# Security
bandit[toml]>=1.7
detect-secrets>=1.5

# Pre-commit hooks
pre-commit>=3.5
