.env
.DS_Store
__pycache__/
*.pyc
*.db
data/resume.pdf
data/profile.json
data/cookies_*.json
data/connections.json
data/campaign_state.json

# Dev / test
.venv/
.pytest_cache/
.coverage
# Dev-only npm tree for the extension's JS test suite (package.json is private,
# nothing here ships — the extension loads no npm code). The lockfile IS tracked.
node_modules/

# Build artifacts
hiredrop-extension.zip
store-assets/*.zip
